<!DOCTYPE html>
<html>
<head>
  <title>CPC Registration</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  
</head>
<body>
	<section id="mainContainer">
<header>
	<ul>
		<li class="" id="white_logo"><%= image_tag "logo/white_logo.gif" %> </li>
		
	</ul>
	
 
</header>
<section id="dialog"> </section>

<section id="container">
	<%= yield %>
	
</section>
</section>
  <script>
  	$(function(){
        $('.check').on('click', function(e){
        e.preventDefault();
         alert("Popup")
        $('<div/>', {'class':'myDlgClass', 'id':'link-'+($(this).index()+3)})
        .html($('<iframe/>', {
            'src' : $(this).attr('href'),
            'style' :'width:100%; height:100%;border:none;'
        })).appendTo('body')
        .dialog({
            'title' : $(this).text(),

            'width' : 484,
            'height' : 590,
            'modal' : true,
            buttons: [ { 
                    text: "Close",
                    click: function() { $( this ).dialog( "close" ); } 
                } ]
        });
    });
});
// function openDialog(){
	// $("#dialog").dialog({modal:true})
// }

    // $(document).ready(function() {  
// 
        // $("#dialog").load(this.getTrigger().attr("href")).dialog({
            // autoOpen: false,
            // width: 400,
            // modal: true
        // });     
        // $(".test").click(function(){$( "#dialog" ).dialog( "open" );});
// });
  	

  </script>
</body>
</html>
